schema {
    query: Query
    mutation: Mutation
}

type User {
    id: ID!
    email: String!
    name: String
    avatarUrl: String
}

type Query {
    users: [User!]!
}

type Mutation {
    # add user to the DB
    addUser(email: String!, name: String, avatarUrl: String): User
}
